1. The keys public.pem and private.pem have been used for encryption purposes. The keys public1.pem and private1.pem are only for testing the KeyGen code have not been used anywhere and will be overwritten when keygen function is called(with proper path specified).
2. Sample Inputs:
Correct one:
Some random text.
Incorrect one:
Some more random text.
Note that the encrypted text due to its length messes up the terminal so it has been provided via input of path through another text file "Encrypted.txt" (It is same for both inputs).